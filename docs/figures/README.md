# Figuras

`industrial_gateway_architecture.drawio` es la fuente normativa de la arquitectura de bloques de Rev. A.

El archivo `industrial_gateway_architecture.svg` debe regenerarse directamente desde Draw.io después de abrir y verificar visualmente la fuente. La exportación automática intentada el 2026-09-15 no generó un resultado nuevo verificable; por eso no se conserva el SVG antiguo, que todavía mostraba la plataforma como `Selection TBD`.

No deben tratarse como documentos normativos los archivos editoriales `.$*.bkp`; están excluidos mediante `.gitignore`.
